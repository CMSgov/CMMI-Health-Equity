{
  "name": "provider-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "jest",
    "build": "mkdir -p {build/provider-api/jwks,build/provider-api/bulk-output-files} && cp jwks/test-config.json build/provider-api/jwks/ && npx tsc -w -p ./tsconfig.json",
    "build:ci": "mkdir -p {build/provider-api/jwks,build/provider-api/bulk-output-files} && cp jwks/test-config.json build/provider-api/jwks/ && npx tsc -p ./tsconfig.json",
    "dev": "nodemon build/provider-api/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "nodemon": "^2.0.15",
    "ts-jest": "^27.1.3",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "@typegoose/typegoose": "^9.5.0",
    "axios": "^0.26.0",
    "express": "^4.17.2",
    "jsonwebtoken": "^8.5.1",
    "jwk-to-pem": "^2.0.5",
    "mongodb": "^4.3.1",
    "mongoose": "^6.1.7"
  }
}
